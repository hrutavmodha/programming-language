Analyze the project and add the detailed README. Do not load previous versions of README by misusing the Git, nor attempt to even view that README contents, because then you will copy paste that docs. You are strictly prohibited from using any Git commands for this task. After adding README, run git diff and git status. Analyze the changes, stage all the changes, commit with brief and appropriate message and push to remote. You are only supposed to use the commands that are asked to run, and not run any other git command on your own. Also, do not lazily and blindly copy paste the test case in src/index.ts as the example of language. Instead, construct valid syntax on your own and write it as an example. Do not went to hallucinating the syntax of my programming language either.